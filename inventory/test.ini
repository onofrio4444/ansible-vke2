# ============================================================
# Un solo nodo "all-in-one"
# (control plane + workload sulla stessa VM)
# ============================================================

[server]
# Metti qui l'IP della VM.
test-node ansible_host=192.168.114.124

[all:vars]
# Utente SSH con sudo sulla VM (modifica se serve).
ansible_user=student01
ansible_become=true
# ansible_ssh_private_key_file=~/.ssh/id_rke2



